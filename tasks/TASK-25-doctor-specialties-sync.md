# TASK-25 — пустой список специальностей тоже синхронизируется

Находка приёмки прогона `g_20260808-173152`, **P2**: при удалении последней специальности
карточка сохраняет `specialties = []`, но общая запись врача обновляется только для
непустого списка ([directories.js:59]). В итоге `/doctor-cards` сообщает, что специальностей
нет, а `/doctors`, шаблоны недели и АРМ оператора продолжают показывать прежнюю — данные
расходятся ровно там, где TASK-19 их и сводила.

## Файлы

- `stubs/api/directories.js`
- `stubs/api/specialties-sync.test.js`

## Вход

—

## Готовность

- [x] Правка специальностей синхронизируется в обе стороны при любом составе списка, включая
      пустой: «пусто» — это значение, а не отсутствие значения.
- [x] Тест доказывает: удалили последнюю специальность → `/doctors` и карточка отдают
      одинаковый (пустой) состав; вернули одну → снова совпадают.

## Проверки

```
npx --no-install vitest run stubs/api/specialties-sync.test.js
npx --no-install eslint ./src --max-warnings=0
```
