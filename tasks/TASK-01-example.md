# TASK-01 — первая задача (шаблон — переписать)

## Файлы

<!-- Что задача имеет право править. Один файл — один владелец: две задачи, независимо
     переписывающие один файл, сводятся арбитром, и это дороже последовательного
     прогона. Файлы вне этого списка харнесс считает расползанием скоупа. -->

- `path/to/file`

## Вход

<!-- Задачи-предшественники (id через запятую) или «—». Планировщик строит по ним волны:
     задачи без общего входа и без пересечения файлов идут одновременно. -->

—

## Готовность

<!-- НЕ БОЛЬШЕ ТРЁХ условий, каждое проверяется КОМАНДОЙ. «UI работает» условием не
     является. Задача с семью условиями не закрывается ни за одну итерацию и крутится
     до потолка. -->

1.
2.
3.

## Проверки

<!-- Конкретные команды, доказывающие каждое условие. Эти же команды нужно продублировать
     в config/checks.json под ключом задачи — там их задаёт владелец, и агент не может
     их сузить до тривиально зелёной. -->

```
```

