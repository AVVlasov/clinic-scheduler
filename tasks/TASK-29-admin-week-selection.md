# TASK-29 — администратор: неделя выбирается, публикация доступна

Находка приёмки прогона `g_20260808-181728`, **P1**: экран фиксирует `weekStart` на текущей
неделе и не даёт выбрать другую ([admin-page.tsx:35]), а стаб при инициализации помечает
именно текущую неделю опубликованной — и кнопка для опубликованной недели заблокирована.
Итог: **основной сценарий администратора «опубликовать расписание» недоступен при любом
обычном запуске.** Экран выглядит рабочим и не работает ни разу.

## Файлы

- `src/pages/admin/admin-page.tsx`
- `src/pages/admin/week-templates.tsx`
- `src/pages/admin/week-selection.test.tsx`

## Вход

TASK-26

## Готовность

1. Неделя выбирается: вперёд как минимум на месяц, назад — в пределах, где это осмысленно;
   выбранная неделя видна и участвует в запросах шаблонов и публикации.
2. Состояние публикации показывается для ВЫБРАННОЙ недели: опубликованная — с датой и без
   активной кнопки, неопубликованная — с доступной публикацией и предупреждением о
   необратимости.
3. Тест доказывает сценарий целиком: при старте на опубликованной неделе публикация
   недоступна, после переключения на следующую — доступна, публикация проходит и итог
   называет числа.

## Проверки

```
npx --no-install vitest run src/pages/admin/week-selection.test.tsx
npx --no-install vitest run src/pages/admin
npx --no-install tsc --noEmit
```
