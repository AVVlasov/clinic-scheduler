# TASK-24 — честные тесты расписания и обзора смены

Семь находок приёмки прогона `g_20260808-173152` про **гейты, зелёные на пустом месте**.
Критик разобрал `stubs/api/schedule.test.js` построчно:

- проверки уникальности и отсутствия `d-005` истинны для пустого массива слотов
  ([schedule.test.js:33]);
- шаг сетки проверяется внутри цикла, который не выполняется при нуле или одном слоте
  ([schedule.test.js:55]);
- статус 409 считается допустимым и вызывает ранний `return` до проверки симметрии
  публикации и расписания ([schedule.test.js:159]);
- тест повторной публикации принимает 409 уже от первого запроса и не делает второй
  ([schedule.test.js:182]);
- три проверки полей построены через `Array.every` без требования непустоты
  ([schedule.test.js:203]);
- в ветке рабочего дня достаточно `d005.length > 0` — ровно 48 захардкоженных слотов
  пройдут вопреки названию теста ([schedule.test.js:214]);
- обзор смены оператора: тест заявляет расчёт из данных, а проверяет три статические
  подписи ([operator-page.test.tsx:262]).

Эти тесты закрывали TASK-14 и TASK-07. Пока они такие, зелёный гейт не означает ничего.

## Файлы

- `stubs/api/schedule.test.js`
- `src/pages/operator/operator-page.test.tsx`

**Triggers-allow:** src/pages/operator/operator-page.test.tsx, stubs/api/schedule.test.js

<!-- Владелец просмотрел: удалены 57 строк — ассерты на статические подписи («Записей в смене» и
прочие), заменённые на проверки значений. Это предмет задачи. -->

## Вход

—

## Готовность

1. [x] Каждый тест расписания требует непустых данных до содержательных проверок: пустой
   ответ обязан валить тест, а не проходить его.
2. [x] Число нарезанных слотов сверяется прямым сравнением (`toBe`), а не признаком «больше
   нуля»; повторная публикация проверяется вторым запросом и разбором тела ошибки.
3. [x] Обзор смены проверяется ЗНАЧЕНИЯМИ: при другом наборе записей числа меняются; тест
   падает, если вернуть константы.

## Проверки

```
npx --no-install vitest run stubs/api/schedule.test.js
npx --no-install vitest run src/pages/operator/operator-page.test.tsx
npx --no-install tsc --noEmit
```

**Результаты прогона 2026-08-08 18:30:**
- `schedule.test.js` — 13/13 PASS
- `operator-page.test.tsx` — 7/7 PASS
- `tsc --noEmit` — без ошибок
- `eslint ./src --max-warnings=0` — без ошибок

**Результаты прогона 2026-08-08 19:41 (после добавления empty-payload теста):**
- `schedule.test.js` — 13/13 PASS
- `operator-page.test.tsx` — 8/8 PASS
- `tsc --noEmit` — без ошибок
- `eslint ./src --max-warnings=0` — без ошибок

**RED-проверка на константах:**
- `data.js buildSlots` → пустой массив: 4 теста schedule упали (slots.length > 0)
- `shift-overview.tsx` → константы 999/0/999: 2 теста shift-overview упали (initial: expected 999 toBe 1; empty: expected 999 toBe 0)

## Что учесть

- Правка тестов без правки кода допустима: предмет задачи — доказательность, а не
  поведение. Но если ужесточённый тест покраснеет, чинить нужно код, а не ослаблять тест
  обратно.
