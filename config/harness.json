{
  "$comment": "Центральный конфиг обвязки. Все проектные значения — здесь; скрипты читают отсюда и не хардкодят. Секретов тут нет: ключи живут в окружении / .env и в git не попадают. Пишется командой `bcf init`, дальше правится руками — это обычный JSON.",
  "engine": "ralph",
  "agent": {
    "$comment": "Бэкенд кодового агента для ЦИКЛА (одна задача, много итераций). {model} подставляется на запуске, промпт идёт на stdin. Для другого CLI поменяй command и положи разборщик его потока в harness/lib/adapters.",
    "command": "opencode run --dangerously-skip-permissions --thinking --format json --model {model}",
    "adapter": "opencode",
    "promptStdin": true
  },
  "models": {
    "$comment": "Модели цикла. Пустая строка = взять то, что настроено в самом бэкенде.",
    "code": "minimax-coding-plan/MiniMax-M3",
    "tester": "",
    "vision": "",
    "judge": ""
  },
  "graph": {
    "$comment": "Движок ГРАФА (harness/graph.ps1). Роли — не синонимы: экономика роя держится на том, что рамочных решений мало и они дорогие, а исполнения много и оно дешёвое. Одна модель на всё = планировочная цена за каждый токен исполнения. Проверить проводку живьём: bcf doctor.",
    "backends": {
      "$comment": "Заполняется `bcf init` по результатам опроса установленных CLI. Каждый бэкенд — своя команда и свой формат событий (format выбирает разборщик в harness/lib/graph-runtime.ps1).",
      "claude": {
        "command": "claude -p --output-format stream-json --verbose --permission-mode acceptEdits --model {model}",
        "format": "claude",
        "env": [
          "CLAUDE_CODE_OAUTH_TOKEN"
        ],
        "probeModel": "sonnet",
        "authCheck": "claude auth status",
        "authOkPattern": "\"loggedIn\"\\s*:\\s*true"
      },
      "opencode": {
        "command": "opencode run --dangerously-skip-permissions --thinking --format json --model {model}",
        "format": "opencode"
      },
      "codex": {
        "command": "& 'C:\\Users\\Andrey\\AppData\\Local\\OpenAI\\Codex\\bin\\68de26ad08be95cd\\codex.exe' exec --json --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox -m {model}",
        "format": "codex"
      },
      "codex-ro": {
        "command": "& 'C:\\Users\\Andrey\\AppData\\Local\\OpenAI\\Codex\\bin\\68de26ad08be95cd\\codex.exe' exec --json --skip-git-repo-check -s read-only -m {model}",
        "format": "codex"
      },
      "cursor": {
        "command": "cursor-agent -p --output-format stream-json --force --trust --model {model}",
        "format": "cursor",
        "authCheck": "cursor-agent status",
        "authOkPattern": "Logged in as"
      }
    },
    "roles": {
      "$comment": "Три правила, за которые тут заплачено осознанно: (1) critic сидит НЕ на модели worker — несколько агентов одной модели на одном контексте приходят к одной ошибке и подтверждают её друг другу; (2) planner и critic из разных семейств, чтобы критик не защищал решение собственного планировщика; (3) роль, обязанная только смотреть, живёт на бэкенде с песочницей только на чтение.",
      "planner": {
        "backend": "claude",
        "model": "opus"
      },
      "arbiter": {
        "backend": "claude",
        "model": "opus"
      },
      "critic": {
        "backend": "codex-ro",
        "model": "gpt-5.6-sol"
      },
      "worker": {
        "backend": "opencode",
        "model": "minimax-coding-plan/MiniMax-M3"
      }
    }
  },
  "paths": {
    "$comment": "Пути относительно корня ПРОЕКТА. Состояние прогонов живёт в .bcf/ и в git не попадает.",
    "tasks": "tasks",
    "verdicts": "tasks/.verdicts",
    "currentFocus": "tasks/CURRENT-FOCUS.md",
    "bugs": "tasks/.bugs"
  },
  "productPaths": [
    "src"
  ],
  "$productPaths_comment": "Что считается продуктовым кодом. Отсюда берётся детекция «прогресса нет» и расползания скоупа, и по этому же списку ревью решает, что вообще смотреть. Ошибка здесь означает, что правки в доках засчитываются как работа над задачей.",
  "generatedFiles": [
    "package-lock.json"
  ],
  "$generatedFiles_comment": "Файлы, воспроизводимые сборкой (локи, автогенерация). Их грязность в основном дереве НЕ блокирует слияние: один изменённый лок-файл однажды завалил слияние семи задач подряд, каждая из которых уже дошла до PASS.",
  "taskIdPattern": "[A-Za-z][A-Za-z0-9]*-\\d+",
  "taskIdPrefix": "TASK",
  "backpressure": {
    "$comment": "Быстрая проверка после КАЖДОЙ итерации, до верификации. Пустой массив = пропустить. Тяжёлым прогонам здесь не место. Команда, которой не существует, делает гейт красным всегда — и агент начинает чинить несуществующее.",
    "typecheck": []
  },
  "tests": {
    "$comment": "Чем доказывается закрытая задача. runner: cargo|vitest|jest|pytest|go|custom. Пресеты команд и разбора вывода — в harness/require-tests.ps1; здесь можно переопределить любое поле. Без runner гейт «фича доказана тестами» не работает: общий прогон тестов зелен и на пустом месте.",
    "runner": "",
    "command": "",
    "packageFlag": "",
    "passedPattern": "",
    "failedPattern": ""
  },
  "net": {
    "$comment": "Сторож соединения: probeHost = хост API модели. Пустой probeHost выключает сетевой watchdog целиком.",
    "probeHost": "",
    "probePort": 443
  },
  "timeouts": {
    "iterSec": 900,
    "streamStallSec": 150,
    "netWaitMaxSec": 0,
    "$netStallRetryMax_comment": "Ретраи итерации не тратят бюджет, поэтому их потолок = сколько времени можно сжечь впустую. При зависаниях по ~10 минут восемь попыток это 80 минут на одну итерацию.",
    "netStallRetryMax": 3,
    "stepSec": 1800,
    "silentSec": 600,
    "$testerSilentSec_comment": "Порог тишины для LLM-шагов верификации. Ниже основного: агент может законно молчать во время долгой сборки, а тестер либо стримит, либо его стрим завис.",
    "testerSilentSec": 120
  },
  "limits": {
    "$parallel_comment": "taskConcurrency — сколько задач очереди идут одновременно (1 = последовательно). Реальный ограничитель тут не CPU, а пересечение файлов: задача не стартует, пока её файлы держит другая. agentConcurrency — общий потолок агентских процессов: параллелизм упирается в rate-limit провайдера раньше, чем в машину.",
    "taskConcurrency": 1,
    "agentConcurrency": 4,
    "fanoutConcurrency": 2,
    "testerConcurrency": 3,
    "maxIterations": 40,
    "noProgressLimit": 3,
    "sprawlFileLimit": 40,
    "sprawlIterLimit": 5
  },
  "judge": {
    "name": "judge",
    "outputSuffix": "-judge"
  },
  "features": {
    "$comment": "Опциональные подсистемы. Выключены по умолчанию, чтобы цикл ехал без внешних зависимостей. memory требует pgvector + сервер эмбеддингов (bcf memory init). vision требует визуальный харнесс и дизайн-эталон: включать её без них значит получить зелёный гейт на пустоте.",
    "memory": false,
    "bugSupervisor": false,
    "hardCap": false,
    "fanout": false,
    "vision": false,
    "visualContext": false,
    "referenceDigest": false,
    "redoTrigger": false
  },
  "envPrefix": "BCF"
}
