# clinic-scheduler

<!--
Этот файл — правила проекта. Его читает и кодовый агент, и приёмка, поэтому всё, что
здесь написано, становится основанием блокировать работу. Обратное тоже верно: чего
здесь нет, того нельзя требовать на ревью — это будет спор о вкусах за твои деньги.

Файл создан `bcf install`. Разделы ниже — заготовка: заполни их, лишнее удали.
-->

## Что это за продукт

<!-- Одно-два предложения: что делает и для кого. Если есть docs/PRD.md — здесь ссылка
     на него, а не пересказ. -->

TODO

## Стек и границы

<!-- Что разрешено. Вариант, нарушающий этот раздел, отклоняется без обсуждения —
     именно поэтому раздел стоит заполнить до первого прогона, а не после. -->

- Языки/фреймворки:
- Явно НЕ используем:

## Где что лежит

<!-- Достаточно карты верхнего уровня. Полное дерево устареет через неделю. -->

| Каталог | Что там |
|---------|---------|
|         |         |

Продуктовый код перечислен в `config/harness.json` → `productPaths`. Всё, чего там нет
(доки, скрипты, обвязка), правками по задаче не считается: харнесс не признаёт это
прогрессом.

## Как проверяется работа

- Быстрая проверка после каждой итерации: `config/harness.json` → `backpressure.typecheck`
- Раннер тестов: `config/harness.json` → `tests`
- Обязательные проверки задачи: `config/checks.json`

Правило: **закрытая задача доказывается командой, а не осмотром.** «Работает» — не
условие готовности; «такая-то команда даёт такой-то результат» — условие.

## Правила, которые дороже всего нарушить

<!-- Сюда попадает то, что уже ломалось. Пустой раздел — признак того, что он ещё не
     оплачен опытом; заполняй по мере первых инцидентов. -->

- Моки и фейковые интеграции в продуктовом коде запрещены: экран, которому подложили
  данные, выглядит рабочим и проходит визуальную приёмку.
- Тест без ассерта на поведение хуже отсутствующего: он делает гейт зелёным.

## Задачи

- Файлы задач: `tasks/<PREFIX>-<NN>-<slug>.md`, текущая — `tasks/CURRENT-FOCUS.md`
- Вердикты: `tasks/.verdicts/<ID>.md` (пишет обвязка, не агент)
- Состояние прогонов: `.bcf/` (в git не попадает)
